/**
 * Types specific to OData
 */
export enum ODataType {
    /**Represents a single calendar day (e.g., 1/1/1970) */
    DateTime = "datetime",
    /**Represents a single calendar day (e.g., 1/1/1970) */
    Date = "date",
    /**Represents a GUID */
    Guid = "guid",
}